use std::path::Path;

pub fn verify_pkg_config(file: &Path) -> Result<(), String> {
    Ok(())
}

/// Tars the directory and compresses it into a .fpkg
pub fn package_pkg(dir: &Path, out: &Path) -> Result<(), String> {
    Ok(())
}
